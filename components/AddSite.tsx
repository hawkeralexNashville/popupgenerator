"use client";import {useState} from "react";import {useRouter} from "next/navigation";
export function AddSite(){const [open,setOpen]=useState(false),router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),r=await fetch("/api/sites",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:f.get("name"),url:f.get("url")})});if(r.ok){setOpen(false);router.refresh()}}return <>{<button onClick={()=>setOpen(!open)}>+ Add website</button>}{open&&<form className="card form" style={{marginTop:15}} onSubmit={submit}><label>Website name<input name="name" required/></label><label>Website URL<input name="url" type="url" placeholder="https://example.com" required/></label><button>Add website</button></form>}</>}
