"use client";
import { useState } from "react";import { useRouter } from "next/navigation";import { createSupabaseBrowserClient } from "@/lib/supabase/client";
export function PasswordForm({reset=false}:{reset?:boolean}){const [message,setMessage]=useState(""),[error,setError]=useState(""),router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),supabase=createSupabaseBrowserClient();if(reset){const {error}=await supabase.auth.updateUser({password:String(f.get("password"))});if(error)return setError(error.message);router.push("/dashboard")}else{const {error}=await supabase.auth.resetPasswordForEmail(String(f.get("email")),{redirectTo:`${location.origin}/auth/callback?next=/reset-password`});if(error)return setError(error.message);setMessage("If an account exists, a reset link is on its way.")}}return <form className="card form" onSubmit={submit}><label>{reset?"New password":"Email"}<input name={reset?"password":"email"} type={reset?"password":"email"} minLength={reset?10:undefined} required/></label>{error&&<p className="error-text">{error}</p>}{message&&<p className="success-text">{message}</p>}<button>{reset?"Update password":"Send reset link"}</button></form>}
