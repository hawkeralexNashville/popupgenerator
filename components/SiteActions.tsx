"use client";import {useState} from "react";import {useRouter} from "next/navigation";
export function SiteActions({siteId}:{siteId:string}){const [msg,setMsg]=useState(""),router=useRouter();async function campaign(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),r=await fetch(`/api/sites/${siteId}/campaigns`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:f.get("name")})});if(r.ok)router.push(`/campaigns/${(await r.json()).id}`)}async function bee(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),r=await fetch(`/api/sites/${siteId}/integration`,{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify({apiKey:f.get("apiKey"),publicationId:f.get("publicationId")})});setMsg(r.ok?"Beehiiv connected":(await r.json()).error)}return <div className="grid"><form className="card form" onSubmit={campaign}><h3>Create campaign</h3><label>Name<input name="name" placeholder="General newsletter" required/></label><button>Create and design</button></form><form className="card form" onSubmit={bee}><h3>Connect Beehiiv</h3><label>Publication ID<input name="publicationId" required/></label><label>Private API key<input type="password" name="apiKey" required/></label><button>Verify & connect</button>{msg&&<span>{msg}</span>}</form></div>}
