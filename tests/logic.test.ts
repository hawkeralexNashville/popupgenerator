import {describe,expect,it} from "vitest";import {assignVariant,eligible,suppressionExpiry} from "@/lib/logic";
describe("campaign eligibility",()=>{const target={articleOnly:true,includeUrls:["https://site.test/posts/*"],excludeUrls:["*/sponsored/*"],categories:["food"],tags:[]};it("applies includes, exclusions, article state and taxonomy",()=>{expect(eligible(target,{url:"https://site.test/posts/tacos",article:true,categories:["food"],tags:[]})).toBe(true);expect(eligible(target,{url:"https://site.test/posts/sponsored/tacos",article:true,categories:["food"],tags:[]})).toBe(false);expect(eligible(target,{url:"https://site.test/posts/tacos",article:false,categories:["food"],tags:[]})).toBe(false)})});
describe("equal stable assignment",()=>{it("is stable and approximately even",()=>{const ids=["a","b","c"],counts={a:0,b:0,c:0};for(let i=0;i<3000;i++)counts[assignVariant(`visitor-${i}`,"campaign",ids) as keyof typeof counts]++;expect(Math.max(...Object.values(counts))-Math.min(...Object.values(counts))).toBeLessThan(100);expect(assignVariant("same","campaign",ids)).toBe(assignVariant("same","campaign",ids))})});
describe("frequency",()=>{it("calculates duration and distinguishes session",()=>{expect(suppressionExpiry("days",7,1000)).toBe(1000+7*86400000);expect(suppressionExpiry("session",7,1000)).toBeNull()})});
