import crypto from "node:crypto";import { NextResponse } from "next/server";import { z } from "zod";import { db } from "@/lib/db";import { rateLimit } from "@/lib/rate-limit";
const schema=z.object({siteId:z.string(),campaignId:z.string(),variantId:z.string(),type:z.literal("IMPRESSION"),idempotencyKey:z.string().min(8).max(100),device:z.enum(["mobile","desktop"]).optional(),path:z.string().max(500).optional(),referrerHost:z.string().max(200).optional()});
export async function POST(req:Request){const ip=req.headers.get("x-forwarded-for")?.split(",")[0]||"local";if(!rateLimit(`event:${ip}`,120))return new NextResponse(null,{status:202});const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:"Invalid event"},{status:400});const variant=await db.variant.findFirst({where:{id:p.data.variantId,campaignId:p.data.campaignId,campaign:{site:{publicId:p.data.siteId},status:"ACTIVE"}}});if(!variant)return NextResponse.json({error:"Invalid event"},{status:400});const abuseHash=crypto.createHash("sha256").update(`${new Date().toISOString().slice(0,10)}:${ip}`).digest("hex");await db.event.create({data:{siteId:(await db.site.findUniqueOrThrow({where:{publicId:p.data.siteId}})).id,campaignId:p.data.campaignId,variantId:p.data.variantId,type:"IMPRESSION",idempotencyKey:p.data.idempotencyKey,device:p.data.device,path:p.data.path,referrerHost:p.data.referrerHost,abuseHash}}).catch(()=>null);return new NextResponse(null,{status:202,headers:{"Access-Control-Allow-Origin":"*"}})}
export async function OPTIONS(){return new NextResponse(null,{headers:{"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"content-type","Access-Control-Allow-Methods":"POST, OPTIONS"}})}
