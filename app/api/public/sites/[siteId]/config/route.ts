import { NextResponse } from "next/server";import { db } from "@/lib/db";import { frequencySchema,targetingSchema,triggerSchema,variantConfigSchema } from "@/lib/schemas";
export async function GET(_:Request,{params}:{params:Promise<{siteId:string}>}){const {siteId}=await params;const site=await db.site.findUnique({where:{publicId:siteId},select:{publicId:true,configVersion:true,campaigns:{where:{status:"ACTIVE"},orderBy:[{priority:"desc"},{createdAt:"asc"}],select:{id:true,priority:true,trigger:true,targeting:true,frequency:true,variants:{where:{enabled:true},select:{id:true,name:true,config:true}}}}}});if(!site)return NextResponse.json({error:"Not found"},{status:404});const campaigns=site.campaigns.flatMap(c=>{const trigger=triggerSchema.safeParse(c.trigger),targeting=targetingSchema.safeParse(c.targeting),frequency=frequencySchema.safeParse(c.frequency),variants=c.variants.flatMap(v=>{const config=variantConfigSchema.safeParse(v.config);return config.success?[{id:v.id,name:v.name,config:config.data}]:[]});return trigger.success&&targeting.success&&frequency.success&&variants.length?[{id:c.id,priority:c.priority,trigger:trigger.data,targeting:targeting.data,frequency:frequency.data,variants}]:[]});return NextResponse.json({siteId:site.publicId,version:site.configVersion,campaigns},{headers:{"Cache-Control":"public, s-maxage=60, stale-while-revalidate=300","Access-Control-Allow-Origin":"*"}})}
